import React from 'react'
import { LoginForm } from '../../containers'

const Login: React.FC = () => {
  return (
    <React.Fragment>
      <LoginForm />
    </React.Fragment>
  )
}

export default Login
